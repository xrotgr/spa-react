import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#dee683',
    },
    secondary: {
      main: '#dbdebd',
    },
    background: {
      default: 'rgb(234, 234, 234)',
    },
  },
});
