# Posts website

This is a single page application made with React and RTK query for data fetching and caching. There is a home page, where you can see the posts and create your own, about page, page of each individual post with post content and comments.

## Installation

1. Clone the repository:

```bash
 git clone https://github.com/xrotgr/spa-react.git
```

2. Install dependencies:

```bash
 npm install
```

3. Create an .env file and insert VITE_API_URL variable with a value https://jsonplaceholder.typicode.com

## Usage

To run the project, use the following command:

```bash
npm run dev
```

## List of dependencies

- "@emotion/react": "^11.14.0",
- "@emotion/styled": "^11.14.1",
- "@fontsource/montserrat": "^5.2.8",
- "@mui/material": "^7.3.7",
- "@reduxjs/toolkit": "^2.11.2",
- "@types/node": "^25.1.0",
- "react": "^19.2.0",
- "react-dom": "^19.2.0",
- "react-redux": "^9.2.0",
- "react-router-dom": "^7.12.0",
- "react-toastify": "^11.0.5",
- "typescript": "^5.9.3"
